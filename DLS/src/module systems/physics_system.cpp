#include "module systems/physics_system.hpp"

namespace dls {
	void physics_system::set_velocity(tick& tick, rigidbody& rigidbody, vector<fixed<32>, 3> const& velocity) {

	}

	void physics_system::set_acceleration(tick& tick, rigidbody& rigidbody, vector<fixed<32>, 3> const& acceleration) {

	}
}
