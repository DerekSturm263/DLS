#include "module systems/physics_system.hpp"

namespace dls {
	void physics_system::set_velocity(tick& tick, simulation_agent& rigidbody, vector<fixed<32>, 3> const& velocity) {

	}

	void physics_system::set_acceleration(tick& tick, simulation_agent& rigidbody, vector<fixed<32>, 3> const& acceleration) {

	}
}
