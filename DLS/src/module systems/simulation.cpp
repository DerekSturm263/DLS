#include "systems/simulation.hpp"

namespace dls {
	void simulation::set_velocity(tick& tick, simulation_agent& rigidbody, vector<fixed32, 3> const& velocity) {

	}

	void simulation::set_acceleration(tick& tick, simulation_agent& rigidbody, vector<fixed32, 3> const& acceleration) {

	}
}
