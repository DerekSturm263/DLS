#pragma once

#include "serializable.hpp"

namespace dls {
	class material_settings : public serializable {
		public:
			void save(os& file) const override {

			}

			void load(is& file) override {

			}
	};
}
